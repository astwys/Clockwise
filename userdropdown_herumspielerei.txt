<select name="members">
    {this.props.usersarray.map(function(user) {
        return <option value={user.id} key={user.id}>{user.username}</option>
    })}
</select>

<Select
    name='users'
    options={this.props.userarray}
/>



options = [];

for (var i = 0; i < {this.props.userarray}.length; i++) {
    options.push({ value: userarray[i].id, label: userarray[i].username })
}





<select name="members">
    {this.props.users.map(function(user) {
        return <option value={user.id} key={user.id}>{user.username}</option>
    })}
</select>




<Multiselect data={this.props.users} />
